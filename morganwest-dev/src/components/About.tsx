import React from 'react';
import { Typography, Container } from '@mui/material';

const About: React.FC = () => (
  <Container>
    <Typography variant="h4">About Me</Typography>
    <Typography>Your brief introduction here.</Typography>
  </Container>
);

export default About;